# {{ ansible_managed }}

ui = true

storage "raft" {
    path = "/opt/vault/data"
    node_id = "vault01"
}

listener "tcp" {
  address = "{{ host_ip_address }}:8200"
  tls_disable = true
}

api_addr = "{{ hashi_vault_api_address }}"
cluster_addr = "http://{{ host_ip_address }}:8201"
